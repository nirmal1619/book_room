import 'package:flutter/material.dart';

class RoomDetailsTile extends StatelessWidget {
  const RoomDetailsTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      title: Text("room "),
    );
  }
}